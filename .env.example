# Vzor konfigurace pro mailovou službu kontaktního formuláře.
# Zkopíruj na `.env` a doplň reálné hodnoty (`.env` je v .gitignore).
SMTP_HOST=smtp.forpsi.com
SMTP_PORT=465
SMTP_USER=info@example.cz
SMTP_PASS=tajne-heslo
CONTACT_TO=info@example.cz
MAILER_HOST=127.0.0.1
MAILER_PORT=8090
